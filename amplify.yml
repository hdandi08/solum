version: 1
applications:
  - appRoot: web
    frontend:
      phases:
        preBuild:
          commands:
            - cd web && npm ci
        build:
          commands:
            - npm run build
      artifacts:
        baseDirectory: dist
        files:
          - '**/*'
      cache:
        paths:
          - web/node_modules/**/*
